.chat_side
  .chat_side_side_header
    .chat_side_side_header_box
      %p.chat_side_side_header_box_user_name
        = current_user.name
      %ul.chat_side_side_header_box_menu
        = link_to new_group_path do
          = icon('far','edit',class: 'new_group')
        = link_to edit_user_path(current_user) do
          = icon('fas', 'cog', class: 'icon')

  .chat_side_groups
    - current_user.groups.each do |group|
      .chat_side_groups_group
        = link_to '#' do
          .chat_side_groups_group_name
            = group.name
          .chat_side_groups_group_immediate_meassage
            メッセージはまだありません。
